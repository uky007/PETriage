# 背景
マルウェア解析において最も頻繁に解析する対象はWindows OSの実行形式であるPEフォーマットファイルである．マルウェアは取り扱い注意な危険物であるため，静的解析や表層解析でなど実行する必要がない場合は万が一でもマルウェアを実行できない環境下での解析が安全上望ましい．そのためマルウェア解析者はLinuxやmacOSを利用するが，Linux/maxOS上で動作するネイティブなPE用の表層解析ツールが存在しないという課題がある．このため，アナリストはFlare VMなどのWindows VMマシンを用意し解析ツールをインストールしてそこにファイルを移動してツールを実行するという手間がある．
加えて古来より多くのユーザが表層解析で利用するツールであるPE StudioやCFF Explorerといったツールの機能は統合されておらず，アナリストは複数のツールを立ち上げて同じバイナリを読み取らせる手間が発生する．この課題を解決するマルチプラットフォームで動作する表層解析の機能を統合したネイティブPEリーダ `petriage`（旧名 `readpe`）を開発した．これによりアナリストの環境に依存せず単一のツールで表層解析を完了させることができる．

# ゴール
- RustでPEファイルの表層解析が**完結**できるツールを開発する

# アプローチ
エージェントチームを立ち上げてサーベイと開発を行う

## サーベイ
- 表層解析にはどのようなツールが使用されているか
- 背景の前提(Linux/macOSで使えるPEファイルリーダが少ない，あっても機能が足りない，既存のツールは単体では機能が充足していない)が正しいかどうかの調査
    - 今まで見ている感じだと
- 既存の著名なツールのソースコードが公開されているか
- Windows OS以外でどこまで機能を実装できるか
    - 表層解析は実行の必要はないため同様の機能は実装できるとは考えている
- PE StudioやCFF Explorerなどのツールにはどのような機能が存在するか
- 既存ツールの役割分担を表にする

## 開発
- Rustでクロスコンパイル可能にしてマルチプラットフォーム対応する
- まずはCLIからコマンド `petriage` でreadelf同様に実行できるように
- 結果はファイル出力できるようにする

# Future Work
- ELF対応